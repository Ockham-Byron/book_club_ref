import 'package:flutter/material.dart';

class FinishedBook extends StatelessWidget {
  const FinishedBook({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
