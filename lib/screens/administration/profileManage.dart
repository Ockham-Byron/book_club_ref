import 'package:flutter/material.dart';

class ProfileManage extends StatelessWidget {
  const ProfileManage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Profil"),
    );
  }
}
